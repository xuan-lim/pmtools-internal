# ESG 專案管理工具 (pmtools-internal)

ESG 年度專案健康追蹤工具，以 React + GitHub Pages 建置，無需後端。

🌐 **線上網址：** https://xuan-lim.github.io/pmtools-internal/

---

## 功能

- **儀表板** — 所有專案健康狀態一覽，紅/黃/綠色標示
- **提醒面板** — 自動標示逾期、即將到期、未開始任務
- **甘特時程** — 視覺化全年任務時程，支援預計 / 實際 / CR 三層
- **專案管理** — 可直接編輯任務、狀態、日期，支援 CR 變更請求
- **KPI 自動計算** — 交付準時度、工時優化度（取代 Excel IF 公式）

---

## 本地開發

```bash
npm install
npm run dev
```

---

## 部署

Push 到 `main` 分支後，GitHub Actions 自動建置並部署到 GitHub Pages。

**初次設定：**
1. Repo Settings → Pages → Source: **GitHub Actions**
2. 完成後每次 push 自動更新

---

## 資料管理

所有專案資料存於 `src/data/projects.json`。

**更新流程：**
1. 在工具介面編輯任務與日期（自動存入 localStorage）
2. 前往「設定」→「匯出 projects.json」
3. 下載後覆蓋 `src/data/projects.json`
4. git commit & push → 自動重新部署

---

## 專案結構

```
src/
├── components/
│   ├── Dashboard/      # 總覽儀表板
│   ├── GanttView/      # 甘特時程
│   ├── ProjectDetail/  # 專案與任務管理
│   └── Shared/         # 設定頁面
├── data/
│   ├── projects.json   # 專案資料（來源）
│   └── settings.json   # 預設設定
├── hooks/
│   ├── useProjects.js  # 資料存取 hook
│   └── useAlerts.js    # 提醒計算 hook
└── utils/
    └── kpi.js          # KPI 與日期計算
```
